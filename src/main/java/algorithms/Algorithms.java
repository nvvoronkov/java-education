package algorithms;

public class Algorithms {
    /*public static void main(final String[] args) {
        System.out.println(stringAnalitics("acdfgh", "afh"));
    }

    public static boolean stringAnalitics(final String s, final String t) {
        char[] string = s.toCharArray();
        char[] input = t.toCharArray();
        int count = 0;
        do {
            for (int i = 0; i < s.length(); i++) {
                if (string[i] == input[count]) {
                    count++;
                }
                if (count == t.length()) {
                    return true;
                }
            }
        } while (count < t.length() - 1);
        return false;
    }
*/
}
